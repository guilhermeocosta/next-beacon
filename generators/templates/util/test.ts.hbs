import {{ camelCase name }} from '.';

describe('{{ camelCase name }} util', () => {
  it('should be called correctly', () => {
    {{ camelCase name }}();

    expect({{camelCase name }}).toHaveBeenCalledTimes(1);
  });
});
